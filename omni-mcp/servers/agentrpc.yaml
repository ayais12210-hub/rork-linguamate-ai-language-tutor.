name: agentrpc
enabled: false
command: npx
args:
  - agentrpc
  - start
env:
  AGENTRPC_TOKEN: ${AGENTRPC_TOKEN}
healthCheck:
  type: stdio
  timeoutMs: 12000
scopes:
  - scope:action
limits:
  rps: 3
  burst: 6
  timeoutMs: 30000
