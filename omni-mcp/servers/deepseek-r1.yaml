name: deepseek-r1
enabled: false
command: npx
args: ["MCP-server-Deepseek_R1", "start"]
env:
  DEEPSEEK_API_KEY: "${DEEPSEEK_API_KEY}"
healthCheck:
  type: http
  url: "http://localhost:3011/health"
  timeoutMs: 10000
scopes: ["llm:query"]
limits: { rps: 3, burst: 6, timeoutMs: 45000 }