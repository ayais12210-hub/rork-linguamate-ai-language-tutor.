name: grok
enabled: false
command: npx
args: ["grok-mcp", "start"]
env:
  GROK_API_KEY: "${GROK_API_KEY}"
probe:
  type: stdio
  timeoutMs: 12000
  intervalMs: 10000
limits:
  rps: 2
  burst: 4
timeouts:
  opMs: 60000
  startMs: 30000
retry:
  attempts: 3
  backoffMs: 1000
scopes: ["llm:chat"]