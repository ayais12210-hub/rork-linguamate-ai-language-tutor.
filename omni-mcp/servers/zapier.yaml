name: zapier
enabled: false
command: npx
args: ["zapier-mcp", "start"]
env:
  ZAPIER_NLA_API_KEY: "${ZAPIER_NLA_API_KEY}"
healthCheck:
  type: http
  url: "http://localhost:3006/health"
  timeoutMs: 10000
scopes: ["workflow:execute"]
limits: { rps: 3, burst: 5, timeoutMs: 25000 }