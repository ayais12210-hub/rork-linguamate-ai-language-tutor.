name: firecrawl
enabled: false
command: npx
args: ["firecrawl-mcp-server", "start"]
env:
  FIRECRAWL_API_KEY: "${FIRECRAWL_API_KEY}"
healthCheck:
  type: http
  url: "http://localhost:3000/healthz"
  timeoutMs: 10000
scopes: ["crawl:read"]
limits: { rps: 2, burst: 5, timeoutMs: 30000 }